# Placeholder example Makefile for a minimal flow
# Replace these targets with actual mflowgen/OpenROAD invocations for your node.

.PHONY: all setup fetch-pdk synth place route clean

all: setup fetch-pdk synth place route

setup:
	@echo "Setting up environment (placeholder). Install Docker or native tools for full flows."

fetch-pdk:
	@echo "Fetching or linking to target PDK (placeholder). Replace with git clone or pdk download steps."

synth:
	@echo "Run synthesis step (placeholder)."

place:
	@echo "Run placement (placeholder)."

route:
	@echo "Run routing (placeholder)."

clean:
	@echo "Cleaning example outputs (placeholder)."
	@rm -rf out || true
